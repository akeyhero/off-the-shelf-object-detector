version: '3.7'
services:
  app:
    build:
      context: .
      target: development
    image: off-the-shelf-object-detector-dev:latest
    ports:
      - '${PORT:-5000}:80'
    volumes:
      - ./app:/app
